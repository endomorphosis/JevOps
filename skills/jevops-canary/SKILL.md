---
name: jevops-canary
description: Nested canary packing — pack_canary, landscape_rows, restore_if, run_sampled. Use for inner-walk evidence rows. Never writes Lean. Lake admits.
---

# Canary packing

Modules: `jevops.walk`, `jevops.outer`.

- `pack_canary` / `slim_canary` / `run_sampled`. Budget-dead skips never lake.
- `landscape_rows` compact analysis. `restore_if` writes clone bytes back.
- Clone lookup and warmup JSONL stay in the consumer. Never docker0.
