---
name: jevops-pick
description: Jev beam ranking, leftover sort, pipeline order, draft catalogs. Use for rank_from_answers, leftover drafts, or compose_steps. Never writes Lean. Lake is the oracle.
---

# Pick / beam

Module: `jevops.pick`. FIRE_T=0.7, residual 0.5, leaf 0.45.

- Family×leaf paths, Noul fire, leftover remaining-cut sort.
- `order_pipeline`, `compose_steps`, `rank_leftover`, `leaf_choice_questions`.
- `live_tree` — restrict a family→leaf catalog to available leaves.
- `classification_from_answers` — family+leaf Choices → geo-mean paths / abstain.
- `padded_id` / `unique_capped` / `unique_push` / `numbered_criteria` / `project_items` — draft bags and catalogs.
- `tree_from_items` / `first_apply` — family→kind trees and first matching kernel apply.
- `count_prefix_lines` / `line_stats` — line counts and indent stats (Lean prefixes stay in the consumer).
- `attach_ranked` — join ranked (id, p) with objects.
- `geo_mean` skips None; empty list is 0.0.
- Beam score is not a lake admit. Never docker0.
