---
name: jevops-jsonld
description: JSON-LD 1.1 graph interface; DuckDB optional. Use for put_jsonld, GraphRAG without binding DuckDB. Never writes Lean.
---

# JSON-LD

Module: `jevops.jsonld`. JSON-LD 1.1 is the graph interface.

- `@context` + `@graph`. DuckDB is an optional adapter, never required.
- Used by GraphRAG / board edges. Hits are not lake admits. Never docker0.
