---
name: jevops-skill-tree
description: Hierarchical family→group→skill catalog scored by the random forest. Use for SKILL_TREE, search_with_forest, or CALL ptr://skill/port_skill_tree. Never writes Lean.
---

# Skill tree

Module: `jevops.skill_tree`.

- Families → groups → CALL stems. Live `decision_tree(tactics)` merges in.
- `search_with_forest` writes `ranked_paths` and `pipeline_bias`. JSON-LD on `nca.jsonld`.
- Forest score is not a lake admit. Never docker0.
